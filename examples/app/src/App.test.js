//import { render, screen } from '@testing-library/react';
//import App from './App';
//
////1 it should return canvas and the length should be 1
////2 it's an object with speficic property
////3 it shoud not be null an type of object || dom node
////4 if we pass clickcallback then that property should be avaialble in the return object and its type should be undefined or function
////5 if we pass groupID  then that property should be avaialble in the return object and its type should be undefined or array
//
//test('renders learn react link', () => {
//    render(<App />);
//    const linkElement = screen.getByText(/learn react/i);
//    expect(linkElement).toBeInTheDocument();
//});
